info:
  name: Fabrics - GetPort
  type: grpc
  seq: 30

grpc:
  url: "{{grpcUrl}}"
  method: /go_nd.v1.FabricsService/GetPort
  methodType: unary
  message: |
    {
      "id": "port-id-here"
    }
  auth: inherit
  metadata:
    - name: authorization
      value: "Bearer {{grpcToken}}"
