info:
  name: ComputeNodes - Update
  type: grpc
  seq: 14

grpc:
  url: "{{grpcUrl}}"
  method: /go_nd.v1.ComputeNodesService/UpdateComputeNode
  methodType: unary
  metadata:
    - name: authorization
      value: Bearer {{grpcToken}}
  message: |
    {
      "id": "node-id-here",
      "name": "updated-name",
      "hostname": "updated-hostname.local"
    }
  auth: inherit
