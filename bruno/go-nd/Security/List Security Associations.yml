info:
  name: List Security Associations
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/security/associations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
