info:
  name: List Security Contracts
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/security/contracts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
