info:
  name: List Security Groups
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/security/groups"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
