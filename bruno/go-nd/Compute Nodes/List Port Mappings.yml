info:
  name: List Port Mappings
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/v1/compute-nodes/{{nodeId}}/port-mappings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

vars:pre-request:
  nodeId: ""
