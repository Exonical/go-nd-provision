info:
  name: Delete Compute Node
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/compute-nodes/{{nodeId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

vars:pre-request:
  nodeId: ""
