info:
  name: Get Compute Node
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/compute-nodes/{{nodeId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

vars:pre-request:
  nodeId: ""
