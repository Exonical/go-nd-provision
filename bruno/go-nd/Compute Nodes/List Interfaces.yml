info:
  name: List Interfaces
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/v1/compute-nodes/{{nodeId}}/interfaces"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

vars:pre-request:
  nodeId: ""
