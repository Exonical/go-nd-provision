info:
  name: Update Compute Node
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/compute-nodes/{{nodeId}}"
  auth: inherit
  body:
    type: json
    json: |
      {
        "description": "GPU node"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

vars:pre-request:
  nodeId: ""
