info:
  name: Create Interface
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/v1/compute-nodes/{{nodeId}}/interfaces"
  auth: inherit
  body:
    type: json
    json: |
      {
        "role": "compute",
        "hostname": "node1-compute",
        "ip_address": "10.0.1.10",
        "mac_address": "00:11:22:33:44:55"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

vars:pre-request:
  nodeId: ""
