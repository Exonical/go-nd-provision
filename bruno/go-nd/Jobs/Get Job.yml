info:
  name: Get Job
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/jobs/{{slurmJobId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

vars:pre-request:
  slurmJobId: "12345"
