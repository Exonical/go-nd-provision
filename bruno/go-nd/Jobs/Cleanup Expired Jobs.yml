info:
  name: Cleanup Expired Jobs
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/v1/jobs/cleanup"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
