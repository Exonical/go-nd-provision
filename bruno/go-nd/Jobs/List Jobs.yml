info:
  name: List Jobs
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/jobs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
