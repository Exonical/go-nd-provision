info:
  name: Create Storage Tenant
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/v1/storage-tenants"
  auth: inherit
  body:
    type: json
    json: |
      {
        "key": "tenant-a",
        "name": "Tenant A",
        "storage_network_name": "Storage_Net_TenantA",
        "storage_sg_id": 30001
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
