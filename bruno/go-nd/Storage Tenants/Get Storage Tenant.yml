info:
  name: Get Storage Tenant
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/storage-tenants/{{tenantKey}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

vars:pre-request:
  tenantKey: ""
