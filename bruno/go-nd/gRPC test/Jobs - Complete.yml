info:
  name: Jobs - Complete
  type: grpc
  seq: 5

grpc:
  url: "{{grpcUrl}}"
  method: go_nd.v1.JobsService/CompleteJob
  message: |
    {
      "slurm_job_id": "12345"
    }
  auth: inherit
  metadata:
    - name: authorization
      value: "Bearer {{grpcToken}}"
