info:
  name: ComputeNodes - Delete
  type: grpc
  seq: 15

grpc:
  url: "{{grpcUrl}}"
  method: go_nd.v1.ComputeNodesService/DeleteComputeNode
  message: |
    {
      "id": "node-id-here"
    }
  auth: inherit
  metadata:
    - name: authorization
      value: "Bearer {{grpcToken}}"
