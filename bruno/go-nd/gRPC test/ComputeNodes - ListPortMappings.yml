info:
  name: ComputeNodes - ListPortMappings
  type: grpc
  seq: 16

grpc:
  url: "{{grpcUrl}}"
  method: go_nd.v1.ComputeNodesService/ListPortMappings
  message: |
    {
      "compute_node_id": "node-id-here"
    }
  auth: inherit
  metadata:
    - name: authorization
      value: "Bearer {{grpcToken}}"
