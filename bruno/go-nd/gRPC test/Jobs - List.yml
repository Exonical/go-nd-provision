info:
  name: Jobs - List
  type: grpc
  seq: 2

grpc:
  url: "{{grpcUrl}}"
  method: go_nd.v1.JobsService/ListJobs
  message: "{}"
  auth: inherit
  metadata:
    - name: authorization
      value: "Bearer {{grpcToken}}"
