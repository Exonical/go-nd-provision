info:
  name: Get Fabric
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/fabrics/{{fabricId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

vars:pre-request:
  fabricId: DevNet_VxLAN_Fabric
