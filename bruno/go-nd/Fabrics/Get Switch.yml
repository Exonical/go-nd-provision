info:
  name: Get Switch
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/fabrics/{{fabricId}}/switches/{{switchId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

vars:pre-request:
  fabricId: DevNet_VxLAN_Fabric
  switchId: site1-leaf1
