info:
  name: Sync All Ports
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/api/v1/fabrics/{{fabricId}}/ports/sync"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

vars:pre-request:
  fabricId: DevNet_VxLAN_Fabric
